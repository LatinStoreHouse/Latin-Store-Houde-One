/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Si tenías otras configuraciones, como 'reactStrictMode: true',
  // puedes volver a añadirlas aquí dentro.
};

module.exports = nextConfig;